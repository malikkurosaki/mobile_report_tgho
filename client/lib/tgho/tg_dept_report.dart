import 'package:flutter/material.dart';

class TgDeptReport extends StatelessWidget {
  const TgDeptReport({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Material(
      child: Column(
        children: [
          Flexible(
            child: SingleChildScrollView(
              child: Column(
                children: [
                 
                ],
              ),
            ),
          )
        ],
      ),
    );
  }

  onLoad()async{
    
  }
}